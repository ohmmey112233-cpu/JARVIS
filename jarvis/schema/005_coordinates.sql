-- =============================================================================
-- 005 — พิกัดสำหรับคำนวณเส้นทาง
-- =============================================================================
-- ชื่อสถานที่แบบข้อความ ('จอมทอง เชียงใหม่') ใช้กับ Google Distance Matrix ได้
-- แต่ 'จอมทอง' เป็นชื่ออำเภอทั้งอำเภอ — Google จะเดาจุดกลางอำเภอให้ ซึ่งอาจ
-- คลาดจากบ้านจริงหลายกิโล ทำให้เวลาเดินทางที่รายงานไม่ตรงกับที่เปิด Maps ดูเอง
-- (checklist B4 บังคับว่าต้องตรง)
--
-- จึงเก็บพิกัดแยกจากชื่อ: ชื่อไว้แสดงให้คนอ่าน พิกัดไว้ให้ API ใช้
-- รูปแบบ 'lat,lng' คือรูปแบบที่ Distance Matrix รับตรงๆ
--
-- ⚠️ ยังขาดพิกัดของหอกับโรงเรียน ซึ่งเป็นคู่ที่ digest เช้าใช้จริงทุกวัน
--    (หอ→โรงเรียน) — ตราบใดที่ยังไม่มี ตัวดึงจะใช้ชื่อข้อความไปก่อน
-- =============================================================================

INSERT OR IGNORE INTO preferences (key, value, notes) VALUES
  ('home_latlng', '18.4501443,98.7127309',
   'พิกัดบ้านจอมทอง (โอมให้มา 20 ส.ค. 2026) — ใช้กับ Distance Matrix'),
  ('dorm_latlng', '[ต้องกรอก]',
   '⚠️ พิกัดหอมงฟอร์ตเรสซิเดนซ์ — ต้นทางของ digest เช้าทุกวัน'),
  ('school_latlng', '[ต้องกรอก]',
   '⚠️ พิกัดโรงเรียนมงฟอร์ต — ปลายทางของ digest เช้าทุกวัน');

INSERT OR IGNORE INTO schema_version (version) VALUES (5);
