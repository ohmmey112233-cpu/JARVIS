-- =============================================================================
-- 006 — พิกัดหอกับโรงเรียน (โอมให้มา 20 ส.ค. 2026)
-- =============================================================================
-- ⚠️ ข้อสังเกตที่ต้องเคลียร์ก่อนเชื่อบรรทัด 🚗 ใน digest เช้า:
--
--   พิกัดสองจุดนี้ห่างกัน "165 เมตร" (เดินราว 2 นาที)
--   แต่ jarvis-phase1-kit.md ส่วนที่ 1 เขียนว่า หอ→โรงเรียน ใช้ 11-15 นาที
--   ("🚗 หอ→โรงเรียน ตอนนี้ 12 นาที / ออก 07:15 ถึงสบายๆ")
--
--   สองอย่างนี้ขัดกันเอง เป็นได้ 2 ทาง:
--     ก) หอติดกับโรงเรียนจริง → ตัวเลขใน kit เป็นแค่ตัวอย่างสมมติ
--        แปลว่าบรรทัด 🚗 แทบไม่มีประโยชน์ ควรพิจารณาตัดทิ้งหรือเปลี่ยนเป็นอย่างอื่น
--     ข) พิกัดใดพิกัดหนึ่งผิด (เช่น มงฟอร์ตมีหลายแผนก/หลายวิทยาเขต)
--
--   ยังไม่ตัดสินใจแทน — เก็บค่าที่ได้มาไว้ก่อน (แม่นกว่าชื่อสถานที่แน่นอน)
--   แล้วรอโอมยืนยันว่าเป็นทางไหน ดู docs/setup-when-mac-arrives.md ข้อ 5.2
-- =============================================================================

UPDATE preferences
   SET value = '18.7678368,99.0211007',
       notes = 'พิกัดหอมงฟอร์ตเรสซิเดนซ์ (โอมให้มา 20 ส.ค. 2026) — ⚠️ ห่างจากโรงเรียน 165 ม. ตรวจดู 006',
       updated_at = datetime('now')
 WHERE key = 'dorm_latlng';

UPDATE preferences
   SET value = '18.7667810,99.0200037',
       notes = 'พิกัดโรงเรียนมงฟอร์ต (โอมให้มา 20 ส.ค. 2026) — ⚠️ ห่างจากหอ 165 ม. ตรวจดู 006',
       updated_at = datetime('now')
 WHERE key = 'school_latlng';

INSERT OR IGNORE INTO schema_version (version) VALUES (6);
